<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p" xmlns="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/security 
		http://www.springframework.org/schema/security/spring-security.xsd">

	<http auto-config="true" use-expressions="true" create-session="always">
		<intercept-url pattern="/user/message" access="hasRole('BASIC')" />
		<intercept-url pattern="/user/login**" access="permitAll"/>
		<intercept-url pattern="/user/createUser**" access="hasRole('ADMIN')"/>
		
		
		<form-login
		    login-page="/login"
		    default-target-url="/welcome"
			authentication-failure-url="/login?error"
			username-parameter="username"
			password-parameter="password" />
			
		<logout logout-success-url="/login?logout" invalidate-session="true"  />
		<csrf disabled="true"/>
	</http>

	
	<authentication-manager>
		<authentication-provider>
			<password-encoder hash="bcrypt"/>
				<jdbc-user-service id="userService" data-source-ref="dataSource"
					users-by-username-query="SELECT CLIENT_EMAIL, CLIENT_PASSWORD FROM CLIENT WHERE CLIENT_EMAIL=?"
					authorities-by-username-query="SELECT C.CLIENT_EMAIL, R.ROLE_NAME 
													FROM CLIENT C, ROLE R 
													WHERE C.ROLE_ID = R.ROLE_ID AND C.CLIENT_EMAIL = ?" />
		</authentication-provider>
	</authentication-manager>

	<beans:import resource="spring-config.xml"/>
	
</beans:beans>
